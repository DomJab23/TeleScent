@startuml TeleScent Component Diagram

skinparam backgroundColor #FFFFFF
skinparam defaultFontSize 11

title TeleScent System - Component Diagram

[React Frontend] as frontend
[Express Backend] as backend
[Python ML Service] as ml
database "SQLite\n(Users)" as db
storage "In-Memory\n(Sensor/Predictions)" as memory

node "eNose Device\n(ESP32 + Sensors + GSM)" as enose
node "Scent Emitter\n(PWM + Cartridges)" as emitter

' Main connections
frontend --> backend : HTTP API\nSSE Stream
backend --> db : Auth
backend --> memory : Store Data
backend --> ml : stdin/stdout
enose --> backend : HTTP POST\n(GSM Cellular)
emitter --> backend : HTTP GET Poll\n(every 5s)

note right of enose
  BME680, SGP41, GMXXX
  SIM800L GSM Module
end note

note right of ml
  6-sensor + 2-sensor models
  VOC/NO2 drop detection
  Consecutive logic (3x)
end note

note left of backend
  JWT Auth
  SSE Broadcasting
  Static File Server
end note

@enduml
