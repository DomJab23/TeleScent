# Machine Learning Requirements for TeleScent Scent Detection

# Core ML libraries
scikit-learn>=1.3.0
joblib>=1.3.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# PyTorch (CPU). Required to serve the ScentNet backend; serve.py degrades
# to the sklearn pipeline if torch is unavailable.
torch>=2.0
imbalanced-learn>=0.11

# Visualization (for notebook)
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter support
jupyter>=1.0.0
ipykernel>=6.25.0
