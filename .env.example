# TeleScent Environment Configuration

# Ngrok Configuration (optional - get free token from https://dashboard.ngrok.com/get-started/your-authtoken)
# Without token, ngrok will work but URLs change on restart
NGROK_AUTHTOKEN=your_ngrok_token_here

# Backend Configuration
NODE_ENV=production
PORT=5001
JWT_SECRET=telescent-production-secret-change-this

# Docker Configuration
DOCKER_ENV=true
